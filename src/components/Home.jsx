import React from 'react';
import PropTypes from 'prop-types';

Home.propTypes = {
    
};

function Home(props) {
    return (
        <div className="Home">
            This is Home component
        </div>
    );
}

export default Home;